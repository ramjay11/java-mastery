package abstracts_interfaces;

// Usually, interfaces are adjective words since objects are nouns, if you modify, you'd use adjectives
public interface Drivable {

	double PI = 3.14;
	
	int getWheel();
	
	void setWheel(int numWheels);
	
	double getSpeed();
	
	void setSpeed(double speed);
}
